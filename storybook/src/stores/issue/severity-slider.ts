export const store = {
  sub: {
    'issue.current.user.severity/score': 6,
    'issue.severity/score': 4.5,
    'issue.severity.score/votes': 3000,
  },
  evt: ['issue.current.user.severity/vote'],
};
