import { localeSlice } from '@elements/logic/locale';
import { appSlice } from '@elements/logic/app';
import { routerSlice } from '@elements/logic/router';

export const slices = [localeSlice, appSlice, routerSlice];
