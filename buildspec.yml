version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd lib
      - npm install

  build:
    commands:
      - npx tsc && npx vite build --mode production

artifacts:
  files:
    - '**/*'
  discard-paths: yes
  base-directory: 'lib/dist'